name: Point homepage to mixed LALERTE script

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/force-lalerte-home.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update LALERTE homepage runtime version
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          s=s.replace('assets/js/lalerte-home.js?v=20260915-5','assets/js/lalerte-home.js?v=20260915-7')
          s=s.replace('assets/js/lalerte-home.js?v=20260915-6','assets/js/lalerte-home.js?v=20260915-7')
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit homepage runtime reference
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet || (git commit -m "fix: load mixed Ciwara Infos and LALERTE homepage block" && git push)
