(()=>{'use strict';
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
async function initEmissions(){const section=document.querySelector('#programmes');if(!section)return;let data;try{const r=await fetch('data/programmes.json?v='+Date.now(),{cache:'no-store'});if(!r.ok)return;data=await r.json()}catch(_){return}const items=(data.items||[]).filter(x=>x.active!==false);let slider=section.querySelector('.ciwara-emissions-slider');if(!slider){slider=document.createElement('div');slider.className='ciwara-emissions-slider';const title=section.querySelector('.section-title');if(title)title.insertAdjacentElement('afterend',slider);else section.prepend(slider)}
if(!items.length){slider.innerHTML='<div class="ces-head"><div><span>RADIO CIWARA 105.5 FM</span><b>NOS ÉMISSIONS</b></div><strong>PROGRAMMES</strong></div><div class="ces-empty">Les affiches de vos émissions seront affichées ici.<small>Ajoutez vos émissions et leurs affiches depuis le CMS.</small></div>';return}
const cards=items.map(x=>`<a class="ces-slide" href="${esc(x.link||'#programmes')}">${x.image?`<img src="${esc(x.image)}" alt="${esc(x.name||'Émission')}" loading="lazy">`:''}<span>${esc(x.schedule||'PROGRAMME')}</span><b>${esc(x.name||'Émission')}</b><small>${esc(x.description||x.host||'Radio Ciwara 105.5 FM')}</small></a>`).join('');
slider.innerHTML=`<div class="ces-head"><div><span>RADIO CIWARA 105.5 FM</span><b>NOS ÉMISSIONS</b></div><strong>${items.length} PROGRAMMES</strong></div><div class="ces-track-wrap"><div class="ces-track">${cards}${cards}</div></div><div class="ces-foot"><button type="button" class="ces-prev" aria-label="Émissions précédentes">‹</button><small>Défilement automatique · pause au survol</small><button type="button" class="ces-next" aria-label="Émissions suivantes">›</button></div>`;
const track=slider.querySelector('.ces-track');let offset=0;const step=()=>Math.min(285,slider.clientWidth>900?285:250);const move=dir=>{offset=Math.max(0,offset+dir*step());track.style.animation='none';track.style.transform=`translateX(-${offset}px)`};slider.querySelector('.ces-prev').onclick=()=>move(-1);slider.querySelector('.ces-next').onclick=()=>move(1)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initEmissions,{once:true});else initEmissions();
})();